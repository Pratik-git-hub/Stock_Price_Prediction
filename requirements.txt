install latest versions of
numpy ,
pillow,
pandas,
nsepy,
tensorflow,
sklearn,
matplotlib,


for running first create mysql database named 
"stock" and in that database one table as " user (user_id int auto_increment primary key,user_name varchar(20))"